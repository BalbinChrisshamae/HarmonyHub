<div class="row">
    <% blogs.forEach(blog=> { %>

        <div class="col-md-4">
            <div class="card zoom mb-3">
                <a href="#">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-sm-6">
                                <h6 class="badge badge-light border text-dark rounded-pill px-3"><i
                                        class="far fa-circle"></i>
                                    <%= blog.category %>
                                </h6>
                            </div>
                            <div class="col-sm-6 text-right">
                                <i class="fa-solid fa-eye"></i>
                                <%= blog.totalView%>
                            </div>
                        </div>
                        <h5 class="card-title">
                            <%= blog.title%>
                        </h5>
                        <p class="card-text">
                            <%= stripTags(blog.content) %>
                        </p>
                        <p class="card-text text-right font-italic"><small class="text-muted">
                                <%= new Date(blog.created_at).toLocaleString('en-US', { year: 'numeric' ,
                                    month: '2-digit' , day: '2-digit' , hour: 'numeric' , minute: 'numeric' , hour12:
                                    true }) %>

                            </small>
                        </p>
                    </div>
                </a>
            </div>
        </div>

        <% });%>
</div>